workflow health {
  start: CheckHealth

  state CheckHealth {
    // Return health status
    action services/common/response.Response(
      value: {
        message: "<<qs.message ?? json.message ?? 'none'>>",
        status: "healthy",
        service: "kapi",
        version: $args.version,
        buildTime: $args.buildTime
      },
      statusCode: 200
    ) as HealthResponse
    end ok
  }
}
